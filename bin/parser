#!/usr/bin/env ruby

require "bundler/setup"
require "smart_pension_test"

log_file = ARGV.first

log_parser = SmartPensionTest::LogParser.new(log_file)

log_parser.data.each do |log_item|
  puts "path: #{log_item.path}, ip address: #{log_item.ip_address}"
end